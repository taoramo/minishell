norminette

extra files in folders

make rules and relinking

forbidden includes

extra includes

forbidden functions
	EXTRA HISTORY FUNCTIONS!!!

unused code

malloc
	fail checks
	free
	exit_behaviour

vec fail checks
	vec_new, vec_from, vec_resizze, vec_push, vec_copy, vec_get, vec_insert, vec_append, vec_prepend, vec_map, vec_filter, vec_strlcat

other fail checks
